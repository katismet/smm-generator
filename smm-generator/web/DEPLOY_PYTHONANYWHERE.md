# Инструкция по деплою на PythonAnywhere

## 📦 Какие файлы загрузить на PythonAnywhere

### Структура папок на сервере:
```
/home/TestZerocodSMM/mysite/
├── app/                    # ← Весь каталог src_web/app
├── templates/              # ← Весь каталог src_web/templates
├── static/                 # ← Весь каталог src_web/static
├── src/                    # ← Модули из smm_app/src (generators, social_publishers, social_stats, utils, config.py)
├── instance/               # ← Создается автоматически (БД будет создана здесь)
├── requirements.txt        # ← Файл зависимостей
├── wsgi.py                 # ← WSGI конфигурация
└── .env                    # ← Переменные окружения (создайте вручную)
```

## 📤 Файлы для загрузки:

### 1. Из папки `src_web/`:
- ✅ `app/` (вся папка со всем содержимым)
- ✅ `templates/` (вся папка)
- ✅ `static/` (вся папка)
- ✅ `requirements.txt`
- ✅ `wsgi.py` (создан для вас)

### 2. Из папки `smm_app/src/`:
- ✅ `generators/` (вся папка: __init__.py, text_gen.py, image_gen.py)
- ✅ `social_publishers/` (вся папка: __init__.py, vk_publisher.py)
- ✅ `social_stats/` (вся папка: __init__.py, vk_stats.py)
- ✅ `utils/` (вся папка: __init__.py, logging.py)
- ✅ `config.py`
- ✅ `__init__.py`

### 3. НЕ загружайте:
- ❌ `.venv/` (создастся на сервере)
- ❌ `__pycache__/` (создастся автоматически)
- ❌ `instance/site.db` (создастся автоматически)
- ❌ `outputs/` (локальные файлы)

## 🔧 Настройка на PythonAnywhere:

### Шаг 1: Загрузите файлы
Используйте Files или Git:

```
cd ~/mysite
git clone <your-repo> .
# или загрузите через Files → Upload
```

### Шаг 2: Создайте виртуальное окружение
В консоли PythonAnywhere (Bash):
```bash
cd ~/mysite
virtualenv --python=python3.11 venv
source venv/bin/activate
pip install -r requirements.txt
```

### Шаг 3: Настройте WSGI
В разделе Web → WSGI configuration file:
```python
# Убедитесь, что путь правильный:
path = '/home/TestZerocodSMM/mysite'
```

И укажите путь к wsgi.py:
```
/home/TestZerocodSMM/mysite/wsgi.py
```

### Шаг 4: Настройте Static files
В разделе Web → Static files:
- URL: `/static/`
- Directory: `/home/TestZerocodSMM/mysite/static`

### Шаг 5: Создайте .env файл (ОБЯЗАТЕЛЬНО для FLASK_SECRET_KEY)
В консоли:
```bash
cd ~/mysite
nano .env
```

Добавьте (скопируйте готовый ключ):
```
OPENAI_API_KEY=your_openai_key_here
FLASK_SECRET_KEY=zr-foswq16bo_gPhjifsPYHe6iXxnd5rj8JuUAfcf_bO-MVeprfAD3lSvqAujyk70zWiaw4C2JEdpRp3q3cVEg
```

**Важно**: FLASK_SECRET_KEY обязателен для работы сессий и защиты CSRF. Используйте сгенерированный выше или создайте свой командой:
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(64))"
```

**Альтернатива для PythonAnywhere**: Можете добавить переменные через Web → Environment variables вместо .env файла.

### Шаг 6: Перезагрузите приложение
Нажмите кнопку "Reload" в разделе Web

## ⚠️ Важные моменты:

1. **Пути импортов**: Убедитесь, что в `app/content/routes.py` и других файлах пути к `src.*` корректны
2. **База данных**: SQLite файл создастся в `instance/site.db`
3. **Загрузка изображений**: Папка `static/uploads/` должна быть доступна для записи
4. **Отладка**: Логи доступны в разделе Web → Error log

## 🌐 Ваш сайт будет доступен:
- http://TestZerocodSMM.pythonanywhere.com

