# 🔑 Настройка переменных окружения

## Локальная разработка

Создайте файл `.env` в папке `src_web/` со следующим содержимым:

```env
# OpenAI API ключ
OPENAI_API_KEY=your_openai_api_key_here

# Flask Secret Key (для сессий, CSRF защиты)
FLASK_SECRET_KEY=zr-foswq16bo_gPhjifsPYHe6iXxnd5rj8JuUAfcf_bO-MVeprfAD3lSvqAujyk70zWiaw4C2JEdpRp3q3cVEg
```

**⚠️ ВАЖНО**: 
- Файл `.env` содержит секретные данные, НИКОГДА не коммитьте его в Git!
- Сгенерированный выше `FLASK_SECRET_KEY` можно использовать как есть, или сгенерировать новый

## Генерация нового FLASK_SECRET_KEY

Если хотите создать свой уникальный ключ:

**Windows PowerShell:**
```powershell
python -c "import secrets; print(secrets.token_urlsafe(64))"
```

**Linux/Mac:**
```bash
python3 -c "import secrets; print(secrets.token_urlsafe(64))"
```

## PythonAnywhere

На PythonAnywhere у вас есть два варианта:

### Вариант 1: Через .env файл
```bash
cd ~/mysite
nano .env
```
Добавьте те же переменные, что и для локальной разработки.

### Вариант 2: Через Web интерфейс
1. Зайдите в Web → Environment variables
2. Добавьте:
   - `OPENAI_API_KEY` = `your_key_here`
   - `FLASK_SECRET_KEY` = `zr-foswq16bo_gPhjifsPYHe6iXxnd5rj8JuUAfcf_bO-MVeprfAD3lSvqAujyk70zWiaw4C2JEdpRp3q3cVEg`

## Зачем нужен FLASK_SECRET_KEY?

- **Безопасность сессий**: Защищает данные сессии пользователей
- **CSRF защита**: Flask-WTF использует его для защиты форм
- **Логин**: Flask-Login требует его для работы авторизации
- **Без него**: Приложение может работать нестабильно или с ошибками безопасности

